let products = [1, 3, 4, -13, 23, 204, 1];

let value = products.slice(products.length-2, products.length-1);

console.log('-Передосатнній елемент масиву : [' + (products.length-2) + '] = ' + value + ';');